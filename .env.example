# remiaq Environment Configuration

# Server Configuration
SERVER_ADDR=127.0.0.1:8888
ENVIRONMENT=development

# Firebase Cloud Messaging
FCM_CREDENTIALS=./firebase-credentials.json

# Worker Configuration
WORKER_INTERVAL=10